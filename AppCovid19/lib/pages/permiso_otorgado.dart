import 'package:flutter/material.dart';

class PermisoOtorgado extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
//      appBar: AppBar(
//        title:  Text("Solicitud de Permiso"),
//      ),
      body: Stack(
        children: <Widget>[
//          _crearFondo( context ),
//          _seleccionPermiso(context),
        ],
      ),
    );
  }
}
